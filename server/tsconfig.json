{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2020",                          /* Use modern JS features but still compatible with Node.js. */
    "lib": ["ES2020"],                           /* Include standard library files. */
    "module": "CommonJS",                        /* Node.js uses CommonJS. */
    "moduleResolution": "Node",                  /* Match Node.js resolution algorithm. */

    /* Directories */
    "rootDir": "./src",                          /* Your source TypeScript files. */
    "outDir": "./dist",                          /* Where compiled JS goes. */
    "baseUrl": "./src",                          /* Allows absolute imports from src. */

    /* Strictness */
    "strict": true,                              /* Enable all strict type-checking options. */
    "esModuleInterop": true,                     /* Allow default imports from CommonJS. */
    "forceConsistentCasingInFileNames": true,    /* Ensure consistent file naming. */
    "skipLibCheck": true,                        /* Skip type checking of declaration files. */

    /* Node-specific */
    "resolveJsonModule": true,                   /* Import JSON files as modules. */
    "types": ["node", "express"],                /* Add typings for Node.js and Express. */
    
    /* Debugging */
    "sourceMap": true,                           /* Generate .map files for debugging. */
    "noImplicitAny": true                        /* Avoid implicit any types. */
  },
  "include": ["src"],                            /* Compile all TS inside src/ */
  "exclude": ["node_modules", "dist"]            /* Ignore these folders */
}
