{
  "dependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.10"
  },
  "scripts": {
    "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter frontend dev\"",
    "start": "pnpm --filter server start",
    "build": "pnpm --filter frontend build && pnpm --filter server build",
    "postinstall": "cd frontend && pnpm install && cd ../server && pnpm install",
    "dev:codespaces": "nodemon --ext ts,tsx --exec \"pnpm build && pnpm start\""
  }
}
